// Identity values reused across more than one global entry. Defining them once
// here is what keeps the contract a *single* source — e.g. the company name
// appears on the home hero and the about page, but there is exactly one literal
// in code, so the two globals can never disagree.
export const COMPANY_NAME = '安徽国瑞种业有限公司';
